#include <stdio.h>
/**
 * print_rev - prints string in reverse
 * @s: The string to be printed in reverse order
 */

void print_rev(char *s)
{
	int len = strlen(str);

	for (len = 0; strlen(str);
			for (i = len - 1; i >= 0; i--);
			}
